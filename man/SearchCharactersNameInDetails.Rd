% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basement.R
\name{SearchCharactersNameInDetails}
\alias{SearchCharactersNameInDetails}
\title{按性别与种族搜索角色多语言名称}
\usage{
SearchCharactersNameInDetails(
  Gender = c("Male", "Female"),
  Species = c("Dragon", "Human"),
  Language = c("Chinese", "Japanese", "Romaji")
)
}
\arguments{
\item{Gender}{性别筛选条件，允许 \code{"Male"}（男）或 \code{"Female"}（女）。
默认同时包含两者。}

\item{Species}{种族筛选条件，允许 \code{"Dragon"}（龙）或 \code{"Human"}（人类）。
默认同时包含两者。}

\item{Language}{返回结果的语言，可选 \code{"Chinese"}（中文名）、
\code{"Japanese"}（日文名）或 \code{"Romaji"}（罗马音）。}
}
\value{
字符向量，包含符合条件的角色名称。若无匹配则返回零长度向量并给出警告。
}
\description{
从角色信息表中按性别和种族筛选角色，并以指定语言返回角色名列表。
}
\examples{
# 查找所有龙族女性角色的中文名
SearchCharactersNameInDetails(
  Gender = "Female", Species = "Dragon", Language = "Chinese"
)

# 查找所有人类男性角色的罗马音
SearchCharactersNameInDetails(
  Gender = "Male", Species = "Human", Language = "Romaji"
)
}
