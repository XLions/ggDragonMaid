% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basement.R
\name{SearchCP}
\alias{SearchCP}
\title{搜索角色CP的罗马音名称}
\usage{
SearchCP(name)
}
\arguments{
\item{name}{长度为1的字符向量，待搜索的角色名称。支持中文名（如 \code{"托尔"}）、
日文名（如 \code{"トール"}）或罗马音（如 \code{"Tohru"}）。要求输入的
角色必须存在于角色信息表中，否则将引发错误。}
}
\value{
一个字符串，表示配对角色的罗马音。例如输入 \code{"托尔"} 返回 \code{"Kobayashi"}，
输入 \code{"小林"} 返回 \code{"Tohru"}。
}
\description{
根据输入的任意语言（中文、日文或罗马音）角色名，在预设的CP配对表中查找
其对应的配对角色，并返回该角色的罗马音。CP配对基于《小林家的龙女仆》中的
主要官配关系：托尔 ↔ 小林、康娜卡姆依 ↔ 才川理子、法夫纳 ↔ 泷谷真、
尔科亚 ↔ 真土翔太、依露露 ↔ 会田竹人。
}
\examples{
# 通过中文名查找CP
SearchCP("托尔")

# 通过罗马音查找CP
SearchCP("Kobayashi")

# 日文名也可以
SearchCP("カンナ·カムイ")
}
